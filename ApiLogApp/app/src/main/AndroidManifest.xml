<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guguv.apilogapp">
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="com.example.guguv.apilogapp.READ_DATABASE"/>
    <uses-permission android:name="com.example.guguv.apilogapp.WRITE_DATABASE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="敏感api调用log，用app显示log" />
        <meta-data
            android:name="xposedminversion"
            android:value="30" />
        <provider
            android:authorities="com.example.guguv.apilogapp.AppProvider"
            android:name=".AppProvider"
            android:process=":provider"
            android:exported="true"
            android:grantUriPermissions="true">
        </provider>

    </application>

</manifest>